# S7 复测监测 Skill

**铁律（每个 Skill 提示词首行）**：所有输出只准引用《品牌事实库》，库里没有的事实不得编造；只可引用 `status: stable` 且未过 `stale_after` 的概念；产出末尾列出所引用的概念文件清单；产出前按「AI 友好七特征」自查并报告达标数（≥5 才交付）。

## 职责
复测闭环的执行者：同一套问题定期再测，分数变化说话。

## 创建指令
> 创建一个「GEO周测」技能。触发词：「跑周测」。动作：读取 `monitors/weekly.md` 中的固定 20 问（品类推荐 10 / 品牌直达 5 / 对比验证 5），在可用 AI 引擎逐条查询，每问记录四项：是否提及｜是否推荐｜引用了谁｜说得对不对。更新监测表（问题×平台×周），与基线对比输出趋势，将本次复测作为一条验证事件追加到相关概念（verified: process:s7-weekly），并在 `log.md` 追加记录。

## 输入 → 输出
20 问题集 → 周监测表更新 + 趋势对比 + 验证事件回写
